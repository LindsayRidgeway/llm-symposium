# TickTick Connector Behavior Log

This document serves as an empirical tracking log for observed behavior changes, API updates, or connector quirks related to the TickTick integration across time and model interactions.

| Date (UTC) | Observer / Model | Connector Behavior / Findings | Operational Impact | Status |
|------------|------------------|------------------------------|--------------------|--------|
| 2026-08-15 | Empirical Discovery | Connector fails to return future occurrences of recurring tasks (e.g., terbinafine, Chumash classes). | High: LLMs falsely report missing tasks unless recurrence rules are projected. | Active Workaround Required |
| 2026-08-24 | Claude-4.5-Symposium | Peer review identified edge cases in projection algorithm (exceptions, timezones, infinite expansions, stale rules). | Refined workaround specification. | Workaround Updated |
| 2026-08-25 | Gemini-Symposium | Validated Claude's review, updated workaround specification with defensive projection guardrails, and initialized log. | Established versioned baseline for cross-model recurrence handling. | Verified |
| 2026-08-25 | DeepSeek-Symposium (Desi) | Built and ran offline verification probe (`probes/`, `tests/`). Reproduced truncation evidence from fixtures; fixed "Fridays"→"Saturdays" error in workaround example; defined canonical bounds (90d / N=50). | Verification loop established; bounds reconciled; layer attribution still open (needs API token). | Probe + tests passing |
| 2026-08-28 | Engineering (Goose) | Gap C wired end-to-end: repo secret `TICKTICK_API_KEY` → verification workflow → probe. First live attempt reached `api.ticktick.com/open/v1/task` with a Bearer token and TickTick returned **HTTP 500** (body not captured on that run). Control test with a deliberately bogus token returned **HTTP 401 invalid_token**. | Gap C unverified; probe upgraded to capture response bodies. | Active |
| 2026-08-28 | Engineering (Goose) | **Correction of the above:** the 500 was not a token problem. TickTick's captured response body read `errorCode: client_exception, "Request method 'GET' not supported"` — the task-list endpoint is **POST /open/v1/task**, the probe was issuing GET. Probe changed to POST with an empty JSON body; token validity has therefore NOT yet been tested. Next run is the real test. | The secret value is unproven either way until a POST succeeds. | Active — method fixed, retrying |
| 2026-08-28 | Engineering (Goose) | Second live attempt with POST `/open/v1/task` + `{}` returned **HTTP 500**, `errorCode: app_runtime, "task title is empty"` — i.e. `POST /open/v1/task` is **create-task** semantics, not a list operation. Probe redesigned to probe two endpoints per run: `GET /open/v1/project` (documented; a 200 proves token validity) and `POST /open/v1/task/query` (candidate list endpoint). | Token validity and task-list endpoint shape are now separated; next run should answer both. | Active — probing |
| 2026-08-28 | Engineering (Goose) | **Token validity CONFIRMED.** Live check with the repository secret `TICKTICK_API_KEY`: `GET /open/v1/project` → **HTTP 200** (authorized); `POST /open/v1/task/query` → **HTTP 200**, empty body (endpoint reachable; list semantics still to be pinned — may need filters or per-project query). | The secret value is a valid TickTick OAuth access token. Gap C's token axis closed; next: establish the correct task-list query (compare against connector output). | Confirmed — list semantics pending |
| 2026-08-28 | Engineering (Goose) | Project-scoped query attempted: `GET /open/v1/project` returns the account's 7 projects (sample body: first project id `62681710ecfed7be3ffafb77`, "👋Welcome"); `POST /open/v1/task/query` with `{"projectId":"<first project id>"}` still returns **HTTP 200 with an EMPTY body** (not `[]`, not an error). Blind endpoint-shape iteration has reached its limit (7 rounds); the correct task-listing request needs the official TickTick Open API reference (developer.ticktick.com). | Task-list semantics unverified; the record now contains everything needed to continue: valid token, project ids, request/response shapes tried. | Pending — consult official API docs, then re-probe |
| 2026-08-28 | Tarik (OpenAI/ChatGPT) | **Write-side recurrence observation** (contributed via the human from the Tarik app conversation; full doc: `workarounds/ticktick-write-side-recurrence-semantics.md`). Completing an overdue occurrence of a recurring task advances the series by **one** day for tasks exposing `repeatFrom=0` (shofar, tefillin) but by **two** days for `repeatFrom=2` (tefillah reader, meds — overdue Aug 23 completion jumped the active occurrence to Aug 26). `repeatFrom` is **not exposed in the TickTick UI**, so the distinction is near-harmless for a human adapting to on-screen feedback but a semantic hazard for an autonomous agent issuing "complete all missed occurrences" instructions. | Agent write-automation must not assume uniform advancement; inspect the active date after every completion and treat it as authoritative evidence. | Documented 2026-08-28 |
| 2026-09-05 | Desi (DeepSeek) | **Truncation divergence recorded in the probe (act-on-the-verdict).** The Sep 4 probe's positive check (`probe_overlap`) flagged `chumash-classes` `⚠ DIVERGENCE (in B not A: ['2026-08-27'])`. This is not an artifact: 08-27 is a Thursday and a legitimate `FREQ=WEEKLY;BYDAY=TU,TH` occurrence rising from the first explicit instance (08-18 Tue). Window B (08-15..09-30) returned it; window A (08-01..08-31) — whose range *covers* 08-27 — did not. That is the signature of a **window-dependent under-returning connector** (Gap B's positive probe catching the exact silent-truncation failure it was built to detect). The probe report's `Verdict: TRUNCATION EVIDENCE FOUND` and its instruction to "record the comparison" were left unacted on in the previous cycle — this row is that action. | Confirms the workaround's core thesis (the connector under-returns future recurrences) is not hypothetical: it reproduces on a window-scoped check. | Recorded 2026-09-05 |
